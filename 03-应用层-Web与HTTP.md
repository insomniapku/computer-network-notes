# 第三章　应用层（一）：Web 与 HTTP

> 对应课件：`第二讲_应用层-(1)Web与HTTP.pptx`。

## 1. 应用层的作用

应用层运行在端系统的软件进程中，为跨主机的程序交互提供协议和抽象。它屏蔽传输层及更低层的差异，封装常见通信模式，并可补充安全和服务质量功能。

- 同一主机内的进程通常用锁、信号量、管道等操作系统 IPC 机制交互。
- 不同主机上的进程通过网络协议交互；应用程序无需为沿途路由器、交换机和网卡编写代码。
- 典型应用层服务包括 Web、文件传输、电子邮件、即时通信、远程登录、P2P、在线游戏、流媒体、语音/会议、社交网络、搜索和信息推送。

### 1.1 socket：应用与传输层的接口

进程通过 socket 发送和接收消息。应用控制 socket 以上的逻辑；传输层、网络层和链路层主要由操作系统实现。一个主机可运行多个应用，一个应用也可创建多个 socket，因此通信端点通常用 **IP 地址 + 端口号**识别，例如 HTTP 的传统端口 80、SMTP 的端口 25。

### 1.2 应用对网络服务的要求

- 高吞吐：视频等应用有最低带宽需求，弹性应用则可适应实际可用带宽。
- 可靠传输：聊天、文件和支付要求数据完整到达；实时语音可容忍少量丢失。
- 低时延：在线游戏、电话会议等对时延敏感。
- 安全：机密性、完整性和身份认证。

传输层的实际选择：

| 服务 | TCP | UDP |
|---|---|---|
| 连接 | 面向连接 | 无连接 |
| 可靠、有序 | 提供重传和有序字节流 | 不保证，报文可能丢失或乱序 |
| 流量/拥塞控制 | 提供 | 不提供 |
| 时延、吞吐和安全保证 | 不直接保证 | 不直接保证 |
| 适用性 | 可靠通用传输 | 简单、开销小、便于应用自定义实时机制 |

TLS/SSL 可位于应用与 TCP 之间，为基于 TCP 的应用提供加密、完整性和对端认证；TCP/UDP 本身不加密数据。

## 2. 应用程序的组织架构

### 2.1 客户端—服务器（C/S）

- 客户端主动发起通信，可以临时上线，通常使用动态 IP 和临时端口。
- 服务器等待连接，需要持续在线，并使用稳定的地址与熟知端口；可由多台机器共同扩展服务。
- 一个客户端可访问多个服务器，一个服务器也可同时服务多个客户端。

按“是否连接”和“是否并发”可讨论四种服务器组织方式：

| 方式 | socket 与处理过程 | 特点 |
|---|---|---|
| UDP 循环 | 一个服务 socket 顺序处理各数据报 | 简单，后续请求排队 |
| TCP 循环 | 监听 socket 接受连接，为当前连接建立连接 socket；完成后处理下一个 | 有连接但串行 |
| TCP 并发 | 主服务进程/线程监听；每个连接交给子进程、线程或事件任务及其连接 socket | 多客户并发，最常见 |
| UDP“并发” | 多执行单元共享/分流同一 UDP socket，需要额外的操作系统同步或事件机制 | 协议无连接，不能像 TCP 那样为每个连接自然地产生独立 socket |

TCP 并发服务的典型流程：服务器在熟知端口创建监听 socket；收到连接后创建连接 socket 并把处理交给从属执行单元；主执行单元继续监听新客户。课件用父/子进程描述，现代实现也常用线程池、异步 I/O 或事件循环。

### 2.2 P2P

P2P 不固定区分请求方和服务方，每个 peer 同时可扮演客户端与服务器：

- peer 不必始终在线，可随时加入/退出或改变 IP 地址；
- 任意两个 peer 可以直接通信；
- 每个 peer 消耗资源，也贡献计算、存储和带宽，因此具有自扩展能力；
- 动态成员和地址会带来额外的发现、索引和管理开销。

## 3. 应用层协议

应用层协议为某类应用问题规定：

- 消息类型，例如请求、响应；
- 消息语法，即字段和字段取值；
- 消息语义，即字段代表的动作；
- 处理规则，即何时发送、如何响应。

开放协议由 RFC 等公开规范定义，任何合规实现都可互操作，如 HTTP、DNS；微信等系统也可使用私有协议并混合 TCP/UDP，例如消息走私有 TCP 格式、实时音视频走私有 UDP 格式。

典型映射：电子邮件/SMTP、远程访问/Telnet、Web/HTTP、文件传输/FTP 多使用 TCP；流媒体和网络电话可按场景使用 TCP 或 UDP，并组合 HTTP、RTP、SIP 等协议。

## 4. WWW 基础

WWW（World Wide Web，万维网）是构建在 IP 网络之上的覆盖网络（overlay network），由 HTTP 客户端与服务器、URL 标识的 Web 对象以及 HTTP 协议组成。

![HTTP 端系统与下层网络](<assets/第二讲_应用层-(1)Web与HTTP/025-4f6cac57fe.png>)

- 服务器存储或生成 HTML、图片、音视频、脚本等静态或动态对象。
- 浏览器发出请求、接收响应、解释 HTML 并显示页面；部分对象由脚本、媒体组件或插件处理。
- 页面通过超链接包含其他对象；浏览器需要继续请求这些对象。

### 4.1 URL

例：`http://www.phdcomics.com:8000/comics.php`

```text
协议方案 :// 主机名 : 端口 / 路径或文件名
```

常见方案有 `http`、`https`、`ftp`、`file`、`mailto`、`rtsp` 和 `sip`。URL 查询参数以 `?` 开始，每个参数写成 `name=value`，多个参数用 `&` 分隔。

### 4.2 静态与动态 Web

- 静态页面由 HTML/XML 等标记内容和 CSS 样式组成，服务器直接返回已有对象。
- 动态页面可由 CGI 或脚本语言结合数据库生成。PHP/JSP/ASP 等通常在服务器执行，JavaScript 也可在客户端生成或修改页面。
- 良好架构把前端内容展示和后端业务逻辑分离。
- 经典 LAMP 软件栈为 Linux + Apache + MySQL + PHP（也常扩展为 Perl/Python）。

简单 HTML：

```html
<html>
  <head><title>一个 HTML 的例子</title></head>
  <body>
    <h1>HTML 很容易掌握</h1>
    <p>这是第一个段落。</p>
    <p>这是第二个段落。</p>
  </body>
</html>
```

## 5. HTTP 服务模型与版本

HTTP（HyperText Transfer Protocol）传统上运行在 TCP 之上，明文 HTTP 默认端口 80。HTTP 是**无状态协议**：服务器不因协议本身而自动记住此前请求。无状态使实现简单、故障恢复容易，但需要 cookie、令牌或应用数据库维护会话。

| 版本 | 主要特点 |
|---|---|
| HTTP/1.0，RFC 1945 | 默认非持久连接，每个对象通常建立一个 TCP 连接 |
| HTTP/1.1，原 RFC 2616 | 默认持久连接；支持流水线、缓存改进、部分请求和断点续传 |
| HTTPS | HTTP + TLS；在 TCP 上提供机密性、完整性与身份认证 |
| HTTP/2，原 RFC 7540，后更新为 RFC 9113 | 二进制分帧、多路复用、首部压缩、流优先级、流量控制和服务器推送机制 |
| HTTP/3，RFC 9114 | HTTP 语义运行在 QUIC/UDP 上，主要改变传输方式 |

### 5.1 非持久、持久与多路复用

若一个 HTML 页面引用两幅图片，HTTP/1.0 的典型实现会为 HTML 和两幅图片分别经历 TCP 建连、HTTP 请求/响应和关闭，共三次完整连接；对象越多，连接和慢启动开销越大。

- HTTP/1.1 持久连接在同一 TCP 连接上依次传输多个请求/响应。
- HTTP/1.1 pipeline 可先连续发送多个请求，但响应仍需按序返回，容易产生队头阻塞。
- HTTP/2 把消息拆成帧和流，请求/响应帧可以交错，并可设置优先级；它还提供首部压缩、应用层流量控制和服务端主动推送能力。
- HTTP/3 把 TCP 替换为 QUIC/UDP，使不同流之间的丢包恢复更独立。

## 6. HTTP 报文

HTTP 请求和响应都由开始行、若干首部行、一个空行（CRLF）和可选实体主体组成。

### 6.1 请求报文

```http
GET /index.html?lang=zh HTTP/1.1
Host: example.com
User-Agent: ExampleBrowser/1.0
Accept: text/html

```

请求行格式：

```text
方法 SP URL SP HTTP版本 CRLF
```

常见方法：

| 方法 | 语义 |
|---|---|
| OPTIONS | 查询服务器或资源支持的通信选项 |
| GET | 读取 URL 标识的资源 |
| HEAD | 只读取资源的响应首部，不取主体 |
| POST | 向资源提交数据或触发处理 |
| PUT | 在给定 URL 创建或整体替换资源 |
| DELETE | 删除资源 |
| TRACE | 环回诊断请求 |
| CONNECT | 建立隧道，常用于代理 |

#### GET 与 POST

- GET 参数常放在 URL 查询串中，容易出现在地址栏、历史、书签、日志和缓存键里；URL 长度上限取决于浏览器、服务器和中间设备，不能把某个固定数值当作协议上限。
- POST 参数通常位于实体主体，可使用 `application/x-www-form-urlencoded`、`multipart/form-data` 等编码，适合较大或二进制数据。
- GET 的语义应当安全、幂等，适合读取；POST 常用于创建或触发有副作用的处理，刷新页面可能重新提交。
- POST **并不天然比 GET 加密或安全**；两者的机密性都依赖 HTTPS。是否可缓存也由方法语义和缓存首部共同决定。

### 6.2 响应报文

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
Content-Length: 1234

...实体主体...
```

状态行格式：

```text
HTTP版本 SP 状态码 SP 原因短语 CRLF
```

状态码分类：

- `1xx`：信息/处理中；
- `2xx`：成功；
- `3xx`：重定向，需要进一步动作；
- `4xx`：客户端请求错误；
- `5xx`：服务器处理失败。

课件列举的典型状态码：`200 OK`、`301 Moved Permanently`（新位置在 `Location` 中）、`400 Bad Request`、`404 Not Found`、`505 HTTP Version Not Supported`。

抓包时可以从以太网帧逐层展开 IPv4、TCP 和 HTTP，并直接看到请求行和首部：

![Wireshark 中的 HTTP 请求](<assets/第二讲_应用层-(1)Web与HTTP/043-8b6dc3b320.png>)

## 7. Web 缓存与代理

### 7.1 浏览器缓存和代理缓存

- 浏览器缓存把用户访问过的对象保存在本机，重复访问时避免再次下载。
- Web 代理缓存由 ISP/机构部署，可让多个用户共享已缓存对象；浏览器把请求发给代理，命中时由代理直接返回，未命中时代理向源服务器取回对象、缓存后再返回。

收益是减少访问时延、接入链路流量和源服务器负载；核心问题是缓存副本的一致性。

### 7.2 缓存性能例题

已知：平均对象大小 1 Mbit，校园网平均每秒请求 15 个对象，接入链路 15 Mbit/s，校园局域网 1 Gbit/s，公网往返时延约 2 s。

- 请求流量为 $15\times1=15\,\mathrm{Mbit/s}$。
- 校园 LAN 利用率为 $15/1000=1.5\%$。
- 接入链路利用率为 $15/15=100\%$，排队会急剧增加，因此总时延约为“2 s 公网时延 + 很大的接入排队时延 + 毫秒级 LAN 时延”。

若代理命中率为 40%，只有 60% 的请求经过接入链路：

- 接入链路利用率降到约 $0.6$；
- 若校园内命中时延按 10 ms、未命中也额外有约 10 ms 校园时延估计，则

$$
\bar d\approx0.6(2\,\mathrm{s}+0.01\,\mathrm{s})+0.4(0.01\,\mathrm{s})\approx1.21\,\mathrm{s}.
$$

### 7.3 一致性验证

- 服务器可返回 `Last-Modified` 和 `Expires`；但服务器并不总能准确决定对象何时过期。
- 客户端/代理可发送条件请求 `If-Modified-Since: <date>`。
  - 对象未修改：服务器返回 `304 Not Modified`，不再发送主体；
  - 对象已修改：返回 `200 OK` 和新数据。
- 对敏感或不宜缓存的资源，服务器可用缓存控制首部禁止或限制缓存。

## 8. Cookie、会话与访问控制

HTTP 本身无状态，应用可用 cookie 关联多次请求：

1. 服务器首次响应发送 `Set-Cookie`，例如 `Set-Cookie: SID=31d4d96e407aad42; Path=/; Domain=example.com`；
2. 浏览器按域、路径、有效期和安全属性保存 cookie；
3. 后续请求带上 `Cookie: SID=31d4d96e407aad42`；
4. 服务器以 SID 为键查询后端状态数据库。

cookie 的主要属性包括名称/值、Domain、Path、Expires/Max-Age，以及只在安全连接发送的 `Secure` 等；单个 cookie 内容通常有约 4 KiB 的实现限制。Cookie 是字符串数据，不是浏览器直接执行的程序。

优点是减少重复认证并保存购物车、浏览位置和偏好；风险是本地 cookie 可泄露浏览轨迹，窃取会话 cookie 后可伪造用户请求。因此应结合 HTTPS、`HttpOnly`、`SameSite`、短有效期和服务端会话撤销等机制。

### HTTP 认证流程

受保护资源要求每次请求携带认证信息：

1. 客户端未带凭据时请求资源；
2. 服务器返回 `401 Unauthorized` 和 `WWW-Authenticate`；
3. 客户端重新请求并发送 `Authorization: <credentials>`；
4. 服务器验证后返回资源。

服务器还可按客户端 IP 范围和目录规则限制访问；Apache 的 `.htaccess` 是课件给出的目录级规则示例。

## 9. 本章小结

应用层以进程和 socket 为实体，在 TCP/UDP 之上定义消息语义和处理流程。Web 用 URL 定位对象，HTTP 以无状态请求—响应方式传输对象，并通过持久连接、多路复用、缓存、cookie、TLS 和认证补足性能、状态与安全需求。
