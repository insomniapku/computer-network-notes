# 第五章　应用层（三）：P2P、流媒体、CDN、Telnet、FTP 与 SNMP

> 对应课件：`第二讲_应用层-(3)其他应用层协议.pptx`。

## 1. P2P 基础

P2P 系统中的每个实体都是 peer。服务和数据传输尽量去中心化：不依赖长期在线的单一服务器，peer 可直接连接、随时加入/退出并改变 IP 地址。典型应用包括 BitTorrent 文件分享、P2P 视频和早期 Skype。

### 1.1 文件分发时间下界

设服务器持有大小为 $F$ 的文件，要分发给 $N$ 个 peer：

- 服务器上传带宽为 $u_s$；
- peer $i$ 的上传/下载带宽分别为 $u_i,d_i$；
- $d_{\min}=\min_i d_i$；
- 网络内部带宽充足，瓶颈只考虑端点接入。

#### C/S 架构

服务器必须上传 $N$ 份文件，最慢客户端必须下载一份，因此

$$
D_{\mathrm{C/S}}\geq \max\left\{\frac{NF}{u_s},\frac{F}{d_{\min}}\right\}.
$$

服务器上传项随 $N$ 线性增长，成为中心瓶颈。

#### P2P 架构

服务器至少上传一份；最慢客户端仍需下载一份；系统总共需要上传 $NF$，而服务器与全部 peer 的总上传能力为 $u_s+\sum_i u_i$：

$$
D_{\mathrm{P2P}}\geq \max\left\{\frac{F}{u_s},\frac{F}{d_{\min}},\frac{NF}{u_s+\sum_{i=1}^{N}u_i}\right\}.
$$

虽然总数据量随 $N$ 增长，每个新 peer 也贡献上传能力，因此 P2P 自扩展性更好。

#### 例题

若每个 peer 的上传带宽均为 $u$，$F/u=1$ 小时，$u_s=10u$，且 $d_{\min}\geq u_s$：

$$
D_{\mathrm{C/S}}=\frac{N}{10}\ \mathrm{h},\qquad
D_{\mathrm{P2P}}=\max\left\{0.1,\frac{N}{10+N}\right\}\ \mathrm{h}.
$$

C/S 时间随 $N$ 无界线性增长；P2P 的聚合上传能力随 peer 数增加，分发时间趋近 1 小时。

### 1.2 核心问题：资源索引

文件系统要维护“文件 → IP + 端口”，即时通信要维护“用户 ID → IP + 端口”。peer 动态上下线、地址变化，使索引的更新与查找成为 P2P 的核心问题。

| 方法 | 工作方式 | 优点 | 问题 |
|---|---|---|---|
| 中心化索引 | peer 向中心登记地址和资源，查询者先问中心再直连资源方 | 查询简单、准确 | 单点故障、性能瓶颈 |
| Query Flood | peer 与少量邻居形成 overlay；查询递归洪泛，命中沿原路径返回 | 无中心 | 消息量大、可扩展性有限 |
| 混合索引 | 普通 peer 连超级结点；超级结点内集中索引，超级结点间洪泛 | 在效率与去中心化间折中 | 超级结点仍是热点，需要维护层次 |

overlay 的边通常是 TCP 连接，与底层 IP 拓扑不同；“相邻 peer”不等于物理网络相邻。

## 2. 典型 P2P 协议与系统

### 2.1 Gnutella

Gnutella 是纯 P2P 文件分发协议：新 peer Alice 从候选列表尝试与 Bob 建 TCP 连接；Alice 向 Bob 发 `Ping`，Bob 向邻居洪泛，收到者回 `Pong`；Alice 根据 `Pong` 继续建立连接。资源查询同样经 `Query` 洪泛，命中者返回 `QueryHit`，资源本身可通过 HTTP 传输。

### 2.2 BitTorrent

BitTorrent 采用 P2P 数据交换，但传统结构含中心化 Tracker：交换同一文件的 peer 形成 torrent，Tracker 保存活跃 peer 列表，帮助新 peer 发现伙伴。

课件流程：

1. 文件切成约 256 KiB 的 chunk（具体大小由 torrent 元数据决定）；
2. 新 peer 向 Tracker 注册并取得 peer 列表；
3. peer 交换自己拥有的块清单，上传和下载并行；
4. peer 动态加入/退出；取得完整文件后可继续做种，也可能“自私离开”。

优化与激励：

- **稀有块优先**：优先下载副本最少的块，降低某些块消失的风险。
- **tit-for-tat/choking**：Alice 优先向给自己发送最快的 4 个 peer 上传；每约 10 s 重新选择，其余暂时 choke。
- **optimistic unchoke**：每约 30 s 随机尝试一个 peer，寻找可能更好的伙伴并给新 peer 机会。
- 倾向选择上下行能力相近的伙伴；贡献越多，预期得到的回报越多。

现实问题包括公共下载站点可能更快、版权争议、peer 不愿共享、网络封锁和恶意结点。

### 2.3 Skype 与 NAT 中继

早期 Skype 是私有的混合 P2P 协议：普通客户端连接超级结点，超级结点索引“用户名 → IP 地址”。若两个普通客户端受 NAT 或防火墙限制不能直连，可让超级结点充当中继；若各自超级结点不直连，还可能需要第三个超级结点转发。

### 2.4 Maze

北大天网 Maze 是 P2P 文件分享案例，曾有大规模注册和在线用户。其机制包括“所见即所得”的文件访问、积分与排队策略鼓励分享、对非法资源进行惩罚，后来更名为 AmazingStore。

### 2.5 区块链与共识

区块链可看作去中心化、频繁写入的 P2P 账本：peer 保存数据区块，区块含前一区块的哈希指针。核心问题不只是找到数据，还要在开放参与者中决定写权限并形成一致结果。

Raft、Paxos、PBFT 是经典共识算法，但开放、匿名、大规模区块链的信任与故障模型不同。比特币用工作量证明竞争出块权：peer 解计算难题，链分叉时通常沿累计工作量最大的有效链继续。**课件原文把周期写成“每周”，此处按比特币协议的约 10 分钟目标出块间隔校正。**

## 3. 分布式哈希表 DHT 与 Chord

DHT 把 key 集合分散到多个 peer，不依赖中心 Tracker 即可定位“key → peer”。与无结构洪泛相比，它通过结构化 overlay 缩小搜索范围。早期代表有 Chord、CAN、Pastry（2001）和 Tapestry（2004）。

### 3.1 Chord 基本思想

- 对 peer 地址和 key 计算哈希并映射到模 $2^m$ 的环。
- key 由顺时针方向遇到的第一个 peer（successor）负责。
- 最简单查找沿环顺时针前进；完整 Chord 用 finger table 加速。
- peer 加入/退出时更新前驱、后继和数据归属；延迟更新能降低维护开销，但增加查询代价。

![Chord 环上的 key 与 successor](<assets/第二讲_应用层-(3)其他应用层协议/037-09e66d845f.png>)

#### peer 加入

新 peer 加入位置 5 时，通知相邻 peer 更新关系，并从原 successor 迁移现在应由它负责的 key（课件示例为 key 4、5）。若多个 peer 几乎同时加入，可周期性询问后继的前驱并稳定化。

#### peer 退出

- 正常退出：通知前驱/后继，并把数据迁给后继。
- 异常退出：本机数据可能丢失，需要把每份数据复制到多个 peer，并从副本恢复。

#### Dynamo 中的虚拟结点

哈希可能导致物理 peer 分布和负载不均，且机器能力不同。把一个物理 peer 映射成多个分散的虚拟结点，可以更细粒度地搬迁区间并按能力分配负载。Amazon Dynamo、Apache Cassandra 等系统使用了相似的一致性哈希思想。

## 4. 流媒体基础

流媒体把连续音视频压缩编码、打包，经网络发送；接收端重组、解码和播放。它有端到端性能和时序约束，必须按时间连续播放，但通常能容忍少量数据丢失。

| 服务 | 特点与课件时延目标 |
|---|---|
| 点播 | 预录内容边下边播；启动 < 10 s，拖动等 VCR 操作 < 1～2 s |
| 直播 | 边录边上传、边下载边播放；大规模直播常有数秒延迟 |
| 实时交互 | 双/多方实时通信；< 400 ms 通常可接受，VR 可能要求 < 25 ms |

课件给出的代表性“码率/时延”示意值：视频会议约 3 Mbit/s、150 ms；直播约 6 Mbit/s、500 ms；云游戏约 30 Mbit/s、30 ms；XR 约 50 Mbit/s、16 ms。它强调新应用同时追求更高码率和更低时延，并非统一标准门限。

### 4.1 编码与容器

视频编码：

- H.264：兼容性好、应用最广，支持多种分辨率和码率。
- H.265/HEVC：相对 H.264 可在相近质量下降低约 50% 码率，但硬件复杂、许可成本高。
- VP9：Google 推动的开放编码，性能接近 HEVC。
- AV1：开放、免版税目标，压缩效率进一步提高。

音频编码包括 AAC、MP3、Opus。容器负责封装音视频轨道：MP4 兼容性好；MKV 支持多音轨/多字幕；FLV 曾因 Flash 和网络交互流行，现逐渐淘汰。

课件以 H.265 专利池说明标准与专利的关系：H.264 由 MPEG LA 管理，HEVC 曾有多个专利池和较高的按设备许可费用；相关费用是历史商业信息。

### 4.2 服务质量挑战

目标是高清、启动快、平滑、交互及时；现实网络则有带宽受限且波动、时延和抖动、丢包、乱序和设备异构。UDP 高效但不保证可靠、有序或拥塞控制；TCP 保证可靠有序字节流，却不保证带宽/时延，还可能引入等待和重传开销。

## 5. 点播、缓冲与抖动

典型 HTTP 点播过程：浏览器请求含媒体 URL 的元文件；Web 服务器返回元文件；浏览器把 URL 交给播放器；播放器从媒体服务器请求音视频；播放器边下载、解压、按时间戳同步并播放。

发送端可能恒速产生分组，但网络抖动使到达间隔不均。到达即播会因暂时缺包卡顿；客户端先缓存再恒速播放可吸收抖动，但增加启动时延。

缓冲区常用两阈值控制：

- 低于低阈值：即将播空，应提高下载速率或降低码率；
- 高于高阈值：缓存占用过多，可减慢下载或提高码率。

设计要在画质、启动/交互时延和卡顿之间权衡：缓存更大通常更平滑，但延迟更高。

## 6. 实时流媒体协议

### 6.1 RTP 与 RTCP

- **RTP（Real-time Transport Protocol）**通常运行在 UDP 上，提供端到端实时媒体承载和时间/序列信息，但不保证 QoS，也不负责音视频编解码和播放缓存。
- **RTCP（RTP Control Protocol）**与 RTP 配合，周期发送发送方/接收方统计，用于质量监测反馈、媒体同步和成员标识；也使用 UDP，并可在一个 UDP 数据报内复合多个 RTCP 包。

二者常作为更完整实时系统的基础，而不是单独构成完整应用。

### 6.2 RTSP

RTSP（Real-Time Streaming Protocol）是有状态的“互联网遥控协议”，控制初始化、播放、暂停和结束，不定义编码、播放器缓存或媒体在网络层的封装。控制报文可走 TCP 或 UDP，媒体常由 RTP/RTCP 承载。

典型流程：获取元文件 → `SETUP` 建立会话 → `PLAY` 开始指定位置播放 → RTP 传媒体 → `TEARDOWN` 结束；每个 RTSP 请求都有响应。

RTSP 和 RTCP 都是控制协议，但 RTSP 面向用户播放动作、可用 TCP/UDP；RTCP 对用户不可见，面向质量反馈并使用 UDP。

### 6.3 RTMP 与其他协议

RTMP 最初由 Adobe 为 Flash 设计，后来广泛用于直播：

| 维度 | RTMP | RTSP（课件对比） |
|---|---|---|
| 传输 | TCP | TCP 或 UDP，常结合 RTP/RTCP |
| 交互 | 可支持聊天、直播互动等 | 播放、暂停、定位等媒体控制 |
| 开销 | 较大 | 使用 UDP 时较低 |
| 场景 | 直播、实时通信 | IP 摄像头、物联网与点播控制 |

RSVP 可让网络层预留资源，常见于专网。MMS 是微软旧流媒体协议，支持 TCP/UDP 并使用 ASF，逐渐淘汰。随着 **HTML5** 和基于 HTTP 的流媒体普及，专用播放协议数量减少。

### 6.4 WebRTC

WebRTC 是 Google 发起并与标准组织共同推进的浏览器实时通信技术，核心基于 UDP：

- `RTCPeerConnection` 建立和维护端到端连接；媒体用 SRTP/SRTCP。
- `RTCDataChannel` 通过 SCTP 传任意二进制数据。
- DTLS 为 UDP 上的会话提供类似 TLS 的安全性。
- ICE 组合候选路径；STUN 发现公网映射；TURN 在直连失败时中继，从而穿越 NAT/防火墙。

![WebRTC 与传统 Web 协议栈](<assets/第二讲_应用层-(3)其他应用层协议/059-f243415f11.png>)

## 7. 基于 HTTP 的自适应流媒体

### 7.1 DASH、HLS 等

- MPEG-DASH：开放国际标准，支持多种音视频编码和容器。
- HLS：Apple 推动的 HTTP Live Streaming，早期主要使用 MPEG-2 TS，服务于 Apple 生态并已广泛扩展。
- HDS：Adobe 为 Flash 推出的 HTTP Dynamic Streaming，随 Flash 退出而减少。
- Microsoft Smooth Streaming：服务 Silverlight，随 Silverlight 被 HTML5/CSS3/JavaScript 取代而衰退。

DASH 把完整视频切成固定时长（课件给出 2～10 s）的 segment，每段提供多个码率；服务器发布描述各段 URL 的 manifest。客户端根据可用带宽和缓冲状态，为**每一段**选择码率。

### 7.2 ABR 算法

目标是选取接近当前可用带宽的码率：高于带宽会延迟并卡顿，明显低于带宽则浪费资源、画质差；困难在于带宽持续变化。

- **基于吞吐**：用滑动窗口平均估计未来吞吐，选择不高于估计值的最大码率。
- **基于缓冲**：按缓冲区占用决定下一段码率，缓冲低时保守、缓冲高时激进。
- **放弃请求规则**：当前高码率段下载过慢、可能导致卡顿时，终止并改下较低码率版本。

### 7.3 可扩展视频编码 SVC

SVC 在 H.264 基础上把码流分为保证基本可播的基础层和提升帧率/空间分辨率的增强层。带宽不足时只传基础层；带宽富余时追加增强层。它降低卡顿风险、利用剩余带宽，但多层编解码和传输控制更复杂。

![SVC 基础层与增强层关系](<assets/第二讲_应用层-(3)其他应用层协议/066-2b8a21197c.png>)

应用层优化仍受底层限制，还可从传输层做拥塞控制、快速丢包恢复和多路径传输，从网络层做调度、QoS 感知测量和边缘计算。

## 8. 内容分发网络 CDN

单一大型服务器有单点故障、源站过载、网络拥塞和远端长路径问题。CDN（Content Delivery/Distribution Network）把静态内容、流媒体和用户视频副本放到靠近用户的边缘节点，降低时延与拥塞，保护源站并提升可扩展性，且对用户透明。

### 8.1 部署方式

- **广度优先**：深入大量接入网部署许多小节点。课件给出的历史例子包括腾讯云 2100+ 节点、Akamai 2015 年约 24 万台/节点资源分布于 120 多个国家。
- **深度优先**：在少量靠近接入网的关键位置建设大集群。

提供者可能是专业 CDN 公司、内容提供商或 ISP。关键问题是服务器布局和把请求调度到合适副本。

### 8.2 资源选择与重定向

```mermaid
flowchart LR
    U[客户端] --> O[源站/服务入口]
    O -->|HTTP 30x + Location| C1[合适 CDN 节点]
    U -->|DNS 查询| D[负载均衡 DNS]
    D -->|返回就近/低负载地址| C2[CDN 节点]
    C1 -->|缓存未命中| O
    C2 -->|缓存未命中| O
```

- **客户端选择**：服务提供者返回包含多个副本的 manifest，客户端按路径、速率或拥塞选择副本和码率。
- **HTTP 重定向**：源站根据位置/负载选择 CDN，以 30x 和 `Location` 返回新 URL。
- **DNS 辅助重定向**：负载均衡 DNS 收集节点位置和负载，解析为合适 CDN 地址。
- **页面显式重写**：页面直接把静态资源 URL 指向 CDN 域名。

缓存未命中时，CDN 节点从源站取回内容并保存副本。

## 9. Telnet

Telnet 是早期远程登录协议，RFC 854 将其标准化；默认使用 TCP 端口 23。为屏蔽异构终端的键盘、字符和命令格式差异，它定义 NVT（Network Virtual Terminal）：

1. 客户端把本地用户名、口令、命令和字符转换为 NVT 格式；
2. 服务器把 NVT 转成本机格式并执行；
3. 服务器输出转回 NVT，客户端再转成本地显示格式。

Telnet 明文传输凭据和数据，不适合不可信网络；实际远程管理通常使用 SSH。

## 10. FTP 与 TFTP

### 10.1 FTP

FTP（RFC 959）面向异构系统，屏蔽存储格式、目录/文件命名、操作系统命令和访问控制差异；支持交互式访问、文件类型/格式和权限。

FTP 用 C/S 和两个 TCP 连接：

- **控制连接**：服务器监听 TCP 21，整个会话保持，用于命令与响应，不传文件。
- **数据连接**：传目录或文件，用完关闭。课件描述的是主动模式，服务器数据进程从 TCP 20 建立连接；被动模式则由服务器另选端口并等待客户端连接。

服务器主进程监听 21；客户到来后创建控制和数据处理进程；从属进程结束后，主进程继续并发接受其他客户。

### 10.2 TFTP

TFTP（Trivial File Transfer Protocol）是独立于 FTP 的简化协议，使用 UDP、C/S 模式，只支持读/写文件，不提供目录浏览和用户认证；支持 ASCII 或二进制模式。

- 客户先向 UDP 69 发送读/写请求，服务器为后续传输选择新的 UDP 端口。
- 数据以从 1 开始编号的 block 传输；传统数据块含 512 B 数据，最后一块不足 512 B 表示结束。
- 若文件大小恰为 512 B 的整数倍，还要再发一个数据长度为 0 的块作为结束标志。
- 每发一块等待带块号的 ACK；发送方超时未收到 ACK 就重传数据块，接收方超时未收到下一块可重发 ACK。这是停等式可靠传输。

## 11. 网络管理与 SNMP

### 11.1 网络管理目标与 FCAPS

网络管理要有效利用资源、及时发现/处理故障，并保障网络正常高效运行。五类功能为：配置（Configuration）、故障（Fault）、性能（Performance）、计费（Accounting）和安全（Security）管理，合称 FCAPS。

### 11.2 基本实体

- **Manager**：管理进程和用户界面，发起管理任务。
- **Managed Object**：被管理的软硬件系统。
- **Agent**：运行在被管理设备中，与 Manager 交互。
- **MIB**：管理信息库。Agent 管理本地 MIB；多个 Agent 与 Manager 的状态共同形成网络视角。
- **管理协议**：TCP/IP 体系使用 SNMP；OSI 曾定义 CMIP。

### 11.3 SNMP 设计与版本

SNMP 追求简单和低影响：交互操作少，基于 UDP 以降低实现和通信开销，设备增减尽量不影响原系统。版本演进包括 SNMPv1（1988—1990）、SNMPsec（1992）、SNMPv2（1993）和 SNMPv3（2002，显著增强安全与管理能力）。

### 11.4 轮询与陷阱

- **轮询**：Manager 周期发送 `get` 读取状态或 `set` 修改状态。实现简单、流量可控，但设备多时开销大、发现突发故障不够及时。
- **trap**：Agent 在重要事件达到门限时主动报告，经过过滤后只发送少量关键事件。

实际系统通常周期轮询常规状态，并用 trap 报告重大事件。

### 11.5 端口与报文

- Agent 在 UDP 161 接收 `get/set` 并返回 response；Manager 用临时端口发请求。
- Manager 在 UDP 162 监听 Agent 主动发送的 trap/inform。

SNMP 消息封装于 UDP/IP。SNMPv3 报文包含版本、安全参数、上下文引擎 ID、上下文名和 PDU；PDU 中有类型、请求 ID、差错状态、差错索引和一组“对象名—值”变量绑定。

| 操作 | PDU |
|---|---|
| 读 | `GetRequest`、`GetNextRequest`、`GetBulkRequest` |
| 写 | `SetRequest` |
| 响应 | `Response` |
| 通知 | `Trapv2`、`InformRequest` |

### 11.6 能力与局限

SNMP 可提供报文/字节计数、CPU 利用率、内存占用等基本统计；写操作常限于改参数或开关端口。它缺少每条 TCP 连接等细粒度信息，也难以表达复杂网络控制。替代和补充方向包括新型测量算法、软件定义网络、网络功能虚拟化和可编程网络。

## 12. 本章小结

P2P 用参与者聚合资源解决中心瓶颈，但必须处理索引、动态成员和激励；DHT 用结构化哈希提高查询效率。流媒体通过缓存、RTP/RTCP/RTSP、WebRTC、DASH/ABR 和 SVC 在带宽、画质、时延与卡顿之间权衡，CDN 再把内容和调度推向网络边缘。Telnet、FTP/TFTP 和 SNMP 展示了远程终端、文件传输和网络管理三类经典应用层设计。
